# Copia este archivo a .env solo si necesitas variables locales.
# ¡NUNCA subas .env al repositorio!
# En producción, los secretos van en el dashboard de Cloudflare (Settings → Environment variables)
# o como variables de entorno del runner. Este proyecto no usa secretos en el código.

# (Opcional) Variables locales para desarrollo
WEB_PORT=8788
